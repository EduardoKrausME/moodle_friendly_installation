<VirtualHost *:8080>
    ServerName {{DOMAIN}}

    ErrorLog {{BASE_DIR}}/logs/apache-error.log
    CustomLog {{BASE_DIR}}/logs/apache-access.log combined

    # BEGIN MOODLE FRIENDLY MODSECURITY
    <IfModule security2_module>
        SecRuleEngine On
    </IfModule>
    <IfModule security3_module>
        SecRuleEngine On
    </IfModule>
    # END MOODLE FRIENDLY MODSECURITY

    DocumentRoot {{WEBROOT}}

    <Directory {{MOODLE_DIR}}>
        Require all denied
    </Directory>

    <Directory {{WEBROOT}}>
        Options FollowSymLinks
        AllowOverride All
        Require all granted
        Order allow,deny
        Allow from all
    </Directory>

    <FilesMatch \.php$>
        SetHandler "proxy:unix:/run/php-fpm/www.sock|fcgi://localhost/"
    </FilesMatch>
</VirtualHost>
